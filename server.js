"use strict";

function minimalCoins(coins, amount){
    if(isNaN(amount) || amount === null)
    {
       return "The amount must be a number";
        
    }

    if(typeof(coins) !== 'object')
    {
        
    }
}

console.log(minimalCoins(1, null));


